apiVersion: v1
kind: Service
metadata:
  name: {{ include "thread-route-updater.fullname" . }}
  labels:
    {{- include "thread-route-updater.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thread-route-updater.selectorLabels" . | nindent 4 }}
