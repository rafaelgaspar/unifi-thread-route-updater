apiVersion: v1
kind: Service
metadata:
  name: {{ include "unifi-thread-route-updater.fullname" . }}
  labels:
    {{- include "unifi-thread-route-updater.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "unifi-thread-route-updater.selectorLabels" . | nindent 4 }}
